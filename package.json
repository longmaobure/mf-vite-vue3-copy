{
  "name": "mf-vite-vue3-copy",
  "version": "1.0.0",
  "description": "模块联邦 vite+vue3 例子仿写",
  "main": "index.js",
  "scripts": {
    "run:remote": "pushd remote && npm run dev && popd",
    "run:host": "pushd host && npm run dev && popd",
    "run:all": "run-p run:remote run:host",
    
    "install:remote":"pushd remote && npm install && popd",
    "install:host":"pushd nost && npm install && popd",
    "install:all":"run-p "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
